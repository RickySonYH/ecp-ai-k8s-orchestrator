# [advice from AI] 개발자 UI 개발용 Docker 이미지
FROM node:18-alpine

WORKDIR /app

# 패키지 파일들 복사 및 의존성 설치
COPY package.json package-lock.json* ./
RUN npm install

# 소스 코드 복사
COPY . .

# 개발 서버 포트 노출
EXPOSE 3000

# 개발 서버 실행
CMD ["npm", "start"]
